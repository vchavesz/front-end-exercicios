// Saída no console de uma string
console.log('Exibir mensagem no console do navegador!');

// Saída no console de uma operação
console.log(10 + 9);

// Saída no console de uma variável
let nomeDoFuncionario = 'Kaio';
let diaDeTrabalho = 10;

console.log(nomeDoFuncionario);

// Concatenação de strings (ou, soma de strings)
console.log('Olá ' + nomeDoFuncionario + ', seja bem-vindo, ao seu dia de trabalho número ' + diaDeTrabalho);