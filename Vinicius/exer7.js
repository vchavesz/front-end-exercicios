// Escreva um programa que exiba os números de 1 a _n_, sendo _n_ um número máximo inserido pelo usuário;

let Num = prompt('Insira um numero:');

let contador = 1;

while(contador <= Num) {
    console.log(contador); 
    contador++; 
}
